<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <h3>All Tickets</h3>
      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th>Title</th>
            <th>Hours</th>
            <th>Customer</th>
            <th>Status</th>
            <th colspan="1"></th>
          </tr>
        </thead>
        <tbody>
          <% @tickets.each do |ticket| %>
            <tr>
              <td data-label="Title"><%= link_to ticket.title, ticket %></td>
              <td data-label="Hours"><%= ticket.hours %></td>
              <td data-label="Customer"><%= ticket.customer %></td>
              <td data-label="Status"><%= ticket.ticket_status %></td>
              <td data-label="Edit"><%= link_to '<i class="fa fa-pencil-square-o"></i>'.html_safe, edit_ticket_path(ticket) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<hr>

<div class="digg_pagination">
  <%= will_paginate @tickets, :container => false %>
</div>

<hr>

<div class="form-group">
  <%= link_to "New Ticket", new_ticket_path, class: 'btn btn-success btn-block' %>
</div>