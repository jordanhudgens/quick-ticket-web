<% flash.each do |flash_type, message| %>
  <div class="alert <%= bootstrap_flash_class(flash_type) %> fade in">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>